import React from "react";
import { NavLink } from "react-router-dom";

import "../assets/nav-bar.scss"

const StartPage = (props) => {
  return (
    <div>
      <div className="row"><button className="btn-prim"> Start</button></div>
    </div>
  );
};

export default StartPage;
